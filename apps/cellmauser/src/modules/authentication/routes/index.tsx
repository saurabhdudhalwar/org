// import { useNavigate } from "react-router-dom";

import LoginScreen from "../screens/LoginScreen";

const Login = () => {
  // const navigate = useNavigate();

  return <LoginScreen />;
};

export default Login;

// onSuccess={async () => navigate("/app")
